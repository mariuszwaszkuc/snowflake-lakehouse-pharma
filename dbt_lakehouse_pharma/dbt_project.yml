name: dbt_lakehouse_pharma
version: 1.0.0
config-version: 2
profile: dbt_lakehouse_pharma
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  dbt_lakehouse_pharma:
    +materialized: view
